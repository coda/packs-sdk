import type { Format } from '../../types';
import type { PackFormulas } from '../../api';
import type { SyncTable } from '../../api';
export declare const formulas: PackFormulas;
export declare const syncTables: SyncTable[];
export declare const formats: Format[];
