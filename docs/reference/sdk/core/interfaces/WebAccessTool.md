---
title: "Interface: WebAccessTool"
nav: WebAccessTool
note: |-
  This file is autogenerated from TypeScript definitions. Make edits to the comments in the TypeScript file and then
  run "make docs" to regenerate this file.
search:
  boost: 0.1
---

# Interface: WebAccessTool

Tool that provides combined web access capabilities including search and fetch.
Each capability can be independently enabled/disabled and configured with domain filters.

## Example

```ts
pack.addSkill({
  // ...
  tools: [
    {
      type: sdk.ToolType.WebAccess,
      search: {
        domainFilter: {
          mode: sdk.WebAccessDomainFilterMode.AllowList,
          domains: ["stackoverflow.com"],
        },
      },
      fetch: {
        enabled: false,
        domainFilter: {
          mode: sdk.WebAccessDomainFilterMode.BlockList,
          domains: ["evil.com"],
        },
      },
    },
  ],
});
```

## Extends

- `BaseTool`\<[`WebAccess`](../enumerations/ToolType.md#webaccess)\>

## Properties

### fetch?

> `optional` **fetch**: [`WebAccessFetchConfig`](WebAccessFetchConfig.md)

Configuration for web fetch capability.
When omitted, the capability is enabled with no domain restrictions.

***

### search?

> `optional` **search**: [`WebAccessSearchConfig`](WebAccessSearchConfig.md)

Configuration for web search capability.
When omitted, the capability is enabled with no domain restrictions.

***

### type

> **type**: [`WebAccess`](../enumerations/ToolType.md#webaccess)

The type identifier for this tool.

#### Inherited from

`BaseTool.type`
