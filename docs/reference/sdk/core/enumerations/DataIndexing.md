---
title: "Enumeration: DataIndexing"
nav: DataIndexing
note: |-
  This file is autogenerated from TypeScript definitions. Make edits to the comments in the TypeScript file and then
  run "make docs" to regenerate this file.
search:
  boost: 0.1
---

# Enumeration: DataIndexing

Indicates the default indexing (ingestion) behavior for a sync table when setting up a connector.

By default, all sync tables are included in ingestion. Use [DataIndexing.Exclude](#exclude) to
mark a sync table as excluded by default, while still allowing users to opt in.

## Enumeration Members

### Exclude

> **Exclude**: `"exclude"`

The sync table is excluded from ingestion by default, but users can manually opt in.

***

### Include

> **Include**: `"include"`

The sync table is included in ingestion by default.
