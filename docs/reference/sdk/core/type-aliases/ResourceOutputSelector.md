---
title: "Type Alias: ResourceOutputSelector"
nav: ResourceOutputSelector
note: |-
  This file is autogenerated from TypeScript definitions. Make edits to the comments in the TypeScript file and then
  run "make docs" to regenerate this file.
search:
  boost: 0.1
---

# Type Alias: ResourceOutputSelector

> **ResourceOutputSelector** = `string`

A path into a tool result. Use `$` for the entire result, `.` for object properties,
and `[]` for every item of an array (for example, `entries[].content`).
For MCP tools, paths start at the call result (`structuredContent` or `content`).
